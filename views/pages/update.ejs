<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
    <%- include('../partials/header'); %>
</header>

<h1>Update Contact</h1>
<main>
<div class="business-form">
    <div id="business-form">
        <form action="/update/<%= contact._id %>" method="POST">
        <div class="form-group">
            <label for="contactName">Contact Name</label>
            <input type="text" class="form-control" id="contactName" name="contactName" value="<%= contact.contactName %>" required>
        </div>
            <div class="form-group">
            <label for="contactNumber">Contact Number</label>
            <input type="text" class="form-control" id="contactNumber" name="contactNumber" value="<%= contact.contactNumber %>" required>
        </div>
        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" class="form-control" id="email" name="email" value="<%= contact.email %>" required>
        </div>


        <div class="button-container">
            <button type="submit" class="btn btn-primary">Update Contact</button>
            <div>
              <a href="../secure/contacts" class="btn btn-secondary">Back to Contacts</a>
              <a href="../secure/contacts" class="btn btn-secondary">Cancel</a>
            </div>
        </div>
    
    </div>
</div>

</body>
</html> 